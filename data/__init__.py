"""Sample data package — small, open corpora + golden sets so everything runs without paid keys.

Run ``python -m data.prepare`` (or ``make data``) to fetch and prepare everything. See README.md
for the dataset cards.
"""
